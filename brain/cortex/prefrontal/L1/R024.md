### R024: og Brain — Use Correct File Paths for Permissions
- **Strength:** 1
- **Rule:** Use project directory path for Edit tool, NEVER ~/.claude/ path. Use Bash for cross-directory file operations. This includes ALL files under ~/.claude/brain/ when using Write/Edit tools. NEVER use Write tool for brain files even if it seems faster -- it triggers approval prompts every time.

---

## Selective Retrieval Principle (consolidated from former duplicate)

Do NOT fetch all data. Instead:
- Fetch minimum evidence needed to act correctly
- Score by: 0.45×relevance + 0.20×recency + 0.20×importance + 0.10×reliability + 0.05×preference
- Escalate to deeper retrieval only when confidence is low
