---
id: PR049
title: og Brain — Vericity Admin Auth Guard + Refresh Status Tracking
type: procedural
created: 2026-04-10
strength: 3
reinforced: 0
---

## Procedure
- All /api/v1/admin/* endpoints gated by require_admin dependency checking JWT email == ceplisj@gmail.com (403 otherwise)
- dbo.refresh_log table (auto-created via IF OBJECT_ID check) tracks: refreshed_at, rows_inserted, source (manual|db-sync)
- run_rebuild_fact_table(source) is extracted reusable function in admin.py
- GET /admin/refresh-status returns last refresh timestamp + rows + source
- Frontend Admin.tsx checks useAuth() email, shows Access Denied for non-admin
- Refresh status pill in page header: time ago + listing count + source
